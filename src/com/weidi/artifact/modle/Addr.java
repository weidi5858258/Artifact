package com.weidi.artifact.modle;

import com.weidi.dbutil.ClassVersion;

/**
 * CREATE TABLE addr (_id INTEGER PRIMARY KEY,msg_id INTEGER,contact_id INTEGER,address TEXT,
 * type INTEGER,charset INTEGER);
 */
@ClassVersion(version = 1)
public class Addr {
}
